[project]
name = "python-tools"
version = "0.1.0"
description = "Maintenance tools for the Encyclopedia of Combinatorial Structures"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "combstruct>=0.1.0a0,<0.2",
    "openpyxl>=3.1.5",
    "pandas>=2.3.2",
]
